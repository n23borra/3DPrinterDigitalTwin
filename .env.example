# Copie ce fichier en .env (à la racine), puis remplis les valeurs.
# Exemple (Linux/macOS/Git Bash) pour générer un secret robuste :
#   openssl rand -base64 32

# Obligatoire (HMAC JWT : vise >= 32 bytes / 256 bits)
JWT_SECRET=CHANGE_ME_GENERATE_A_STRONG_SECRET

# Optionnel (si tu n'utilises pas l'envoi d'email en dev, laisse vide)
MAIL_USERNAME=
MAIL_PASSWORD=
